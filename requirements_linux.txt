# Project Loods WP3 — Raspberry Pi 5 (16GB), Linux-platform
# Installeer met: pip install -r requirements_linux.txt
# Voor I2C op RPi5: ook nodig op OS-niveau (niet via pip):
#   sudo apt install python3-smbus i2c-tools
#   sudo raspi-config  -> Interface Options -> I2C -> enable
#   i2cdetect -y 1   (controleer dat 0x38 (DHT20) en 0x48 (ADS1115) zichtbaar zijn)

# Hardware / sensoren (scripts/common/sensors.py, scripts/linux_rpi5/sensor_reader.py)
smbus2>=0.4.3
gpiozero>=2.0

# Microfoonopname (scripts/mic_capture.py)
sounddevice>=0.4.6
soundfile>=0.12.1

# Spraakherkenning (scripts/speech/*.py)
openai-whisper>=20231117
vosk>=0.3.45
azure-cognitiveservices-speech>=1.34.0
jiwer>=3.0.3

# Dialoog / cross-device / VAD (scripts/dialogue/*.py)
paho-mqtt>=2.0.0
webrtcvad>=2.0.10
